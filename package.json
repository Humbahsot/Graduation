{
  "name": "graduation-rsvp-api",
  "version": "1.0.0",
  "scripts": {
    "build": "echo 'No build needed for plain HTML files!'"
  },
  "dependencies": {
    "@azure/data-tables": "^13.2.2"
  }
}
